// Service Worker — placeholder to prevent 404
// No offline caching is active in this project.
self.addEventListener('install', () => self.skipWaiting());
self.addEventListener('activate', () => {});
